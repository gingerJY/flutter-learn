import 'package:flutter/material.dart';

class GeekRecomm extends StatelessWidget {
  const GeekRecomm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('geek'),
    );
  }
}
